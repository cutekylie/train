# Follow this to install
Change directory and go into train:
```bash
cd train
```

Then make it:
```bash
sudo make
```

Then install it:
```bash
sudo make install
```

And if you want to remove it, just do:
```bash
sudo make uninstall
```
